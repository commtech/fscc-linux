version 1.0.3 (03/15/2011)
--------------------------------------------------------------------------------
This is nothing more than a change to the README.  Bitbucket instead of Google.

version 1.0.2 (02/17/2011)
--------------------------------------------------------------------------------
Bugfix release targeting the registers API.

	* Fix a bug with the Registers class when it wasn't associated with a port
	* Add error exceptions when a trys to set a readonly register or touch an
	  invalid register


version 1.0.1 (01/07/2011)
--------------------------------------------------------------------------------
Small release to update the versioning to make sure it is in sync with our 
other apps so future releases are smoother.

	* Change from 2 digit versioning to 3 digit versioning x.x -> x.x.x
	
	
version 1.0.0 (01/07/2010)
--------------------------------------------------------------------------------
This is the initial release of the 1.0 pyfscc series.
